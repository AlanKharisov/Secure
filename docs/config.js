// Единственная точка правды для фронта
window.APP_CONFIG = {
    // бекенд и статика работают на одном домене:
    API_BASE: "https://app.world-of-photo.com"
    // если решишь держать бэкенд отдельно (например, Render),
    // то ставь сюда https://<имя-сервиса>.onrender.com
};
